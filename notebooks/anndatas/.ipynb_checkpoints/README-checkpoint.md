# AnnData Preparation Notebooks

Notebooks for combining and integrating the perturbation (hybrid) and control (scFib) datasets.

## Notebooks
- `combine_anndata.ipynb` - Merge individual samples into a single dataset (hashtag-resolved)
- `integrate_hyb_controls.ipynb` - Process and harmonize the merged dataset for downstream analyses